/var/log/scheduler/scheduler.log {
    daily
    missingok
    rotate 30
    compress
    delaycompress
    notifempty
    create 0644 stevecarr stevecarr
    postrotate
        # Send HUP signal to scheduler process if running
        pkill -HUP scheduler 2>/dev/null || true
    endscript
}
